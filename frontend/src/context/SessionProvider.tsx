/**
 * SessionProvider — Context provider that checks /auth/me on mount
 * and exposes session state to the component tree.
 *
 * Placeholder: full implementation in task 8.1
 */
export function SessionProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
