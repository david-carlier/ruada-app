/**
 * LoginPage — Public page with a button that navigates
 * to the backend /auth/login endpoint.
 *
 * Placeholder: full implementation in task 8.3
 */
export function LoginPage() {
  return (
    <div>
      <h1>Login</h1>
      <p>Please log in to access the application.</p>
    </div>
  );
}
