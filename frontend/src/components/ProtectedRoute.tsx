/**
 * ProtectedRoute — Route wrapper that redirects to /auth/login
 * if the session is invalid.
 *
 * Placeholder: full implementation in task 8.2
 */
export function ProtectedRoute({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
